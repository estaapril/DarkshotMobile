import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
    marginHorizontal: -20,
  },
  image: {
    width: "100%",
    height: 300,
    marginBottom: 10,
    borderRadius: 5,
  },
});
